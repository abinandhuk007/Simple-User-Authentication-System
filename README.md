# Simple-User-Authentication-System
**Simple User Authentication System** is a basic full-stack web application with signup and login features. Users can register and log in using a username and password. The frontend uses HTML, CSS, and JavaScript, while the backend uses Java Spring Boot with PostgreSQL to store and verify user data.
